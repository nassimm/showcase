import { DurationShortFormPipe } from './duration-short-form.pipe';

describe('DurationShortFormPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationShortFormPipe();
    expect(pipe).toBeTruthy();
  });
});
